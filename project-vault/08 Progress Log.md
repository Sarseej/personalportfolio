# Progress log

## Current state — recovery checkpoint in progress
The first visual direction was rejected. Its framed object, split layout and single-project identity did not meet the intended ambition. The replacement is the Computational Atelier, an architectural portrait of Sarseej’s broader computer-science practice.

Read-only baseline: portfolio-rebuild remains at 4166846d29b19de9ca6e5973fbd8f4012dd059a2. Working-tree changes match the previously authored prototype, documentation and test evidence. All nine protected deployment hashes pass. No additional user changes were found in the reported working-tree inventory.

Before application edits: recorded the rejection, reasoning, authorized source inventory and replacement decision in the new ADR. Studied the reference case study. Next: build the room, workbench, sculpture and research wall; implement one station focus/return flow; inspect desktop/tablet/mobile screenshots and test fallbacks. Do not populate the full portfolio before visual validation.

Legacy planning recovery remains /Users/userselu/.Trash/personalportfolio-legacy-20260907-071453-623632. No commit or publishing actions have occurred.

## Recovery implementation complete — awaiting visual approval
Removed the current Lens/Observatory components, pedestal illustration, boxed viewport, question navigation and ring emblem. Replaced the entry point and visual system with the full-viewport Computational Atelier. No prior Lens object or interaction remains in the active application.

Delivered: enclosed studio architecture, substantial workbench and dual-display workstation, folded smoked-glass computational sculpture, research wall, blocked systems cabinet, résumé folio and timeline, layered lattice opening and exterior depth. Readable labels and persistent professional navigation remain visible. One complete workstation flow includes a 950 ms camera transition, display activation, semantic four-project selector, Back/Escape and focus return. Sculpture/research have concise previews; fuller functionality remains deferred.

Standard, economy, mobile, reduced-motion, static and no-JavaScript paths are implemented. Real room captures supply responsive fallback posters. No additional dependencies, external assets or model inference were introduced.

Visual inspection completed at 1440×900, 1024×768 and 390×844, including workstation reading and mobile panel states. Interaction/accessibility checks passed with zero reported console errors and zero automated violations. See Testing and Performance and review/atelier/browser-results.json. All protected hashes and deployment diffs remain unchanged. Historical rejected screenshots are separated in review/rejected-showroom/.

Current stop: visual validation of the environment and its primary interaction. Do not proceed to full case-study population, systems relationships, career interactions or interpretability data until this core experience is approved. No commit, push, tag, merge, identity or remote changes were performed.

## Contemporary desk expansion — implementation and visual verification
The accepted studio direction is being extended, not restarted. The current authorized dirty worktree remains on portfolio-rebuild; all nine protected files matched their recorded hashes before changes.

Implemented shared home/projects/career/cv hashes, persistent navigation, separate monitor and paper hitboxes, accessible labels, focus highlighting, browser-history listeners, Escape/Back and retargetable camera travel. Projects now has four evidence-based narratives, three-step explanatory walkthroughs, technical expansion and honest limitations. Career has nine dated nodes and three distinguishable categories. CV contains verified education, four experience entries, projects, skills and public contact, also reused in the print route.

Replaced the former cultural treatment with neutral plaster, oak, charcoal hardware, shelving, acoustic panels, broad windows, a plant and a more rounded chair. Removed the whiteboard and its interaction. No dependency or deployment changes. First screenshot inspection led to softer furniture geometry/grain and a corrected plant scale assignment. Final responsive, interaction and accessibility checks are in progress; no claim of completion until evidence is recorded below.

## Contemporary desk iteration complete — 8 September 2026

Completed the expansion of the accepted studio with Projects on the main monitor, Career on the secondary monitor and CV on the desk paper. Conventional navigation and physical objects use identical hash states. The former cultural theme, whiteboard and former project-navigation wording are absent from the active application. No public PDF or project links were invented.

Final validation passed: lint, TypeScript, production build and static export. Chrome checks passed for all three physical mesh hitboxes and HTML labels, navigation, camera transitions, Back/Escape, browser Back/Forward, rapid clicks, direct links, keyboard activation/focus return, project walkthroughs, timeline selection, mobile detail scrolling, reduced motion, unavailable WebGL, context loss, no-JavaScript content and the print-friendly CV route. Eight automated accessibility scans reported zero violations; browser console/page errors were zero.

Captured and inspected Home, Projects, Career and CV at 1440×900, 1024×768 and 390×844. Tablet monitor fit and mobile Back-button sizing were corrected after inspection. Current screenshots and exact browser results are in review/desk-studio/. Local fallback posters reflect the contemporary room.

All nine protected deployment hashes and their Git diffs remain unchanged. Branch remains portfolio-rebuild at 4166846d29b19de9ca6e5973fbd8f4012dd059a2. No commits, pushes, tags, identity changes, remote changes or dependency installation occurred. The iteration is ready for local review and a user-authored milestone commit.

Remaining optional work: supplied public-safe PDF, verified project links/screenshots, hardware GPU profiling and additional browser/device coverage. These do not block the implemented desk destinations.


## After-hours visual polish — verification in progress
Captured the previous room/focused views before edits in review/before-after-hours. Preserved the accepted navigation, state and camera systems. Restored the full tutoring-to-senior-tutoring Career entry and promotion milestone, retaining it in the CV. Added midnight/walnut materials, blue-violet evening depth, warm local desk/paper light, recessed architectural details, distinct monitor previews, finite monitor activation and semantic interface motion.

First rendered review found background architecture too dark and screen reflections too sharp; adjusted fill and display roughness. Desktop/mobile destination checks, promotion content checks, camera/return paths, history, keyboard, static/no-WebGL and reduced motion passed with zero console errors and zero automated accessibility violations. Final typography/light refinements, poster refresh and final build verification are underway. No protected file or dependency changed.

## After-hours visual polish complete — 8 September 2026

Completed the focused visual/content pass without replacing navigation, state, camera paths or object-to-interface behavior. The room now uses evening architectural lighting, walnut, midnight surfaces, localized monitor/folio emphasis, distinct display previews and finite activation effects. Projects and Career have richer semantic screen styling; CV retains its ivory document treatment. Mathematics Tutor → Senior Mathematics Tutor appears as one dated Career role with a promotion milestone and remains in CV. No teaching object or whiteboard was restored.

Final lint, TypeScript, production build/static export and protected-file verification passed. The complete browser run reported zero console errors and zero violations across nine accessibility scans. Additional final desktop/mobile captures verified the clearer Back control and all three mobile return actions after its cosmetic refinement. Reduced-motion tests cover both camera cuts and disabled timeline effects. Current screenshots, promotion detail and browser results are in review/after-hours; before captures are in review/before-after-hours. Refreshed posters are included in the final static build.

All nine protected deployment files remain byte-for-byte unchanged. No dependencies were installed and no commit, push, merge, tag, identity or remote changes occurred. The branch remains portfolio-rebuild. Ready for user review and a user-authored milestone commit. Hardware GPU profiling and other browser engines remain outside this local software-WebGL verification.

## Living Workstation implementation

The cleaned checkpoint is e0de243f6b647a581636d65e07b50100fefee685 on portfolio-rebuild. The visual branch started at that exact commit. Review captures remain local and excluded from version control.

Implemented an identity-led entrance, Build/Trace/Document lighting, four explanatory project drawings, and connected Career lanes. Tutoring and its promotion remain visible in Career and CV. Existing navigation and camera systems are preserved. Validation is in progress; deployment hashes currently pass for all nine protected paths.

## Warm Aero visual pass — 13 September 2026

Completed on `portfolio-living-workstation`; changes remain uncommitted for visual approval. Recolored the entire scene into eggshell architecture, light wood, sage upholstery, espresso hardware and warm-white daylight. Preserved all scene geometry/transforms, camera director, object hitboxes, navigation, content and mobile interaction model. Projects now has pearl application chrome, location/previous-next controls, existing project navigation and previews, and a persistent selection/technology status strip. Career shares the chrome, cream canvas, espresso routes and raised detail treatment; tutoring promotion remains intact. CV, site navigation, labels, focus, scrollbars, favicon, loading/fallback and both rendered posters match.

Centralized 11 named palette colors plus CSS shadow/highlight mixes and Three.js procedural wood/stone bases. Replaced 394 legacy literal occurrences in the three existing visual stylesheets and room component; the standalone favicon uses matching literals. Audited active routes/components for hex, RGB/HSL, named colors, SVG/canvas/material emissions, gradients, inline styles and shadows. Unmounted legacy components were inspected as outside the rendered route tree and left unchanged. No dependency or system refactoring.

Verification: lint and production build passed. Final existing browser suite passed all 16 recorded checks, including Projects/Career/CV/Home, physical hitboxes, Back/Escape, history, keyboard, rapid navigation, direct entry, desktop/tablet/mobile, reduced motion, WebGL fallback/context loss, print CV return, and no-JavaScript content. Zero console errors; zero violations across nine accessibility scans. Supplemental previous/next project and status checks and isolated static CV return passed. First browser run's print-return timeout occurred during an overlapping rebuild; stable final rerun passed. Supplemental static URL was corrected from `/resume/` to `/resume.html` for the local export server.

Inspected desktop room, Projects, Career, CV, mobile room and Projects, and mobile fallback. Room comparison is unmistakably lighter with retained directional shadows and form separation. `review/warm-aero/comparison.html` compares against the saved prior `review/living-workstation/desktop-home.png`; fresh baseline attempt failed to reach the first local preview server. Screenshots and exact browser/color audits are in `review/warm-aero/` (local review artifacts are ignored by Git). Both posters were refreshed from the actual scene.

All nine protected deployment file hashes and Git diffs pass. Modified-source attribution scan is clean. No commit or push. No known palette inconsistencies in reviewed active views; the supplied image attachment was unavailable, so matching is based on the written palette and atmosphere. Verification used local Chrome with software WebGL; other rendering engines and hardware GPUs were not profiled.

## 2026-09-14 — The Latent Studio, phase 1

Replaced the active Warm Aero Home and Projects direction with a blue-hour glass observatory. The close three-quarter workstation, warm task lamp and paper, dark material response, exterior haze, portfolio-derived filaments and depth-separated dust establish the new composition. The primary monitor carries an OncoLA preview and opens the full-screen Projects chapter. Career, CV, Contact, hash navigation and the canonical project/résumé content remain intact.

Added a deterministic landscape with four supported themes, 13 anchor placements and 54 technology/concept satellite placements. Repeated project anchors explicitly connect the themes they share. Reveal structure exposes the theme key and project labels. This is a curated graph, not a learned projection.

Pointer motion uses refs and damped spherical camera offsets, bounded to ±1.5° yaw and ±0.8° pitch. Mobile uses a separate close crop and tiny ambient drift that pauses during touch. Reduced motion freezes decoration and skips the camera flight. Hidden tabs stop rendering.

Added explicit quiet/sound entry and an original local Web Audio ambience. No context exists before sound consent. A permanent control reports the sound state; quiet preference persists locally. Previously enabled sound still requires a fresh gesture on return. Audio suspends with visibility, handles playback refusal and closes on component unmount. Projects changes the upper texture over approximately 900ms without increasing the master level.

Projects uses an 840ms screen approach followed by a full-viewport semantic chapter. The return fades the chapter while restoring the room. The four existing project narratives, distinct diagrams, stage buttons and technical expansions are preserved. Text now appears without inherited staggered fades. Printed CV text no longer intercepts the paper hitbox; browser tests measure the current camera target before physical-object clicks.

Dedicated browser validation passed Home/Projects navigation, five repeated returns, keyboard activation, focus restoration, direct Projects hashes, all four project selections, mobile touch and overflow checks, reduced motion, fallback access, both sound choices, mute, simulated hidden-tab audio suspension/resume, playback refusal and audio cleanup. No console errors or accessibility violations remained in the dedicated desktop Home/Projects and mobile entrance/Projects scans. Home navigation and sound controls meet the 44px touch-target check.

Review media and validation artifacts are stored outside the repository in a temporary review directory. Only clean production fallback posters were written to `public`. No dependencies or deployment configuration changed. All nine protected SHA-256 values match their starting values. The branch remains `portfolio-living-workstation` at `727d1ed`; no files were staged, committed or pushed.

Remaining limits: the room uses stylized procedural geometry; the screen-to-HTML handoff is not a pixel-for-pixel morph; quality tiers use viewport/device hints rather than continuous GPU benchmarking. Automated mobile checks use browser emulation, and sound still needs listening approval on physical speakers/headphones. Career/CV visual conversion and cleanup of the unused Warm Aero files remain outside this phase.

The full existing browser suite also passed: all three physical mesh hitboxes, navigation and history, rapid retargeting, keyboard/focus handling, 1440×900 / 1024×768 / 390×844 layouts, Career promotion detail, reduced motion, WebGL failure/context loss, print CV and no-JavaScript content. All nine accessibility scans in that suite reported zero violations, with zero console errors. The mobile sound control has a dark backing and stays clear of the retained Career/CV toolbars. Lint, production static export and whitespace checks passed.

A three-second desktop probe using ANGLE Metal on Apple M4 Max recorded a 16.7ms median animation-frame interval (181 samples). A forced SwiftShader comparison recorded 150ms, so software-rendered performance must not be confused with the hardware result. Hidden-tab draw-call instrumentation recorded zero calls. After warm-up and explicit garbage collection, JavaScript heap measurements were 10.05MB before the repeated-return probe, 11.60MB after eight returns and 11.93MB after sixteen. This short probe does not prove absence of every leak or establish performance on physical mobile hardware.

## Signal field prototype — 14 September 2026

Added the standalone `#field` destination, shared repository-backed graph, deterministic selection/filtering, semantic keyboard node list, source-linked relationship panels, mobile detail sheet, and local Instrument Serif / Manrope / IBM Plex Mono typography. Refreshed the production scene posters. Existing project narratives and Career/CV destinations remain intact.

Validation: production static export and lint pass; graph structural verification checks 20 nodes and 22 unique, evidence-bearing edges; the existing browser suite passes 16 regression groups with nine clean accessibility scans. Field checks pass keyboard/touch selection, direct entry, browser history, focus return, reduced motion, five repeated round trips, and four additional accessibility scans. No console errors were recorded. The 390px field has no horizontal overflow. All three local WOFF2 files load from exported assets. Protected hashes remain unchanged for all nine files.

Limitations: the final interactive graph uses a brief SVG/HTML opacity handoff after the real camera passes through the glass; it is not a continuously projected interactive Three.js surface. Mobile suppresses secondary visual labels while preserving accessible node names and selected relationships. Specific Ruskin commit attribution is deferred; only collaborative contribution context and repository-level capabilities are shown. Headless desktop/mobile emulation does not replace physical-device performance testing. Review media remains outside the repository.
## Blue Meadow correction — 15 September 2026

Preserved the studio geometry and canonical portfolio/graph data while replacing the exterior with instanced blue grass and warm functional fireflies. Standalone meadow mode remains live Three.js throughout the glass transition. Plain desktop navigation and a mobile text menu replace the chapter rail; sound and entry controls are borderless. Career is on the prominent monitor, Projects on the secondary monitor, and CV remains on the paper. Added a real PDF download from the canonical CV and exposed the existing verified project repository links.

Validation: lint, production static export, graph verification and whitespace checks pass. The full browser suite passes 16 regression groups with nine clean accessibility scans. Meadow desktop/mobile selection and filtering, keyboard, history, direct entry, reduced motion, static fallback and PDF checks pass with two additional clean accessibility scans. Sound consent, mute, hidden-tab suspend/resume and zero hidden draw calls are verified. Repeated field returns produce no console errors. All nine protected-file hashes match the recorded starting values; canonical content and graph files are unchanged.

Review images and the ordered transition sequence are outside the repository. Production posters show the meadow and correct monitor mapping. Remaining limits: grass is stylized procedural geometry, distant density transitions can still be apparent, software-rendered performance is substantially weaker than native GPU rendering, and mobile validation uses emulation rather than physical devices. Legacy unused visual modules remain cleanup candidates. Nothing is staged, committed or pushed.
